import { getFullUserProfile } from "./src/actions/user-actions";

async function main() {
    console.log("Testing getting full user profile directly");
    // This probably won't work in a script because it relies on the request auth() context
}
main();
